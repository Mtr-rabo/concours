version: '2'
services:
  concoursfonctionpublique-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/concoursFonctionPublique/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=concoursFonctionPublique
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
